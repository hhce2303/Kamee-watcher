{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "main-capability",
  "description": "Default capabilities for the main window. Default-deny; add permissions explicitly.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-open"
  ]
}
